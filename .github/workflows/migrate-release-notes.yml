name: Migrate Release Notes

on:
  push:
    branches:
      - the_way_to_0.3.x
    paths:
      - .github/workflows/migrate-release-notes.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out development branch
        uses: actions/checkout@v4
        with:
          ref: the_way_to_0.3.x
          fetch-depth: 0

      - name: Move Release Notes to Markdown
        shell: python
        run: |
          from pathlib import Path
          import re

          version_tag = 'v0.3.0 BETA - Work in progress'
          ai_notice = (
              '<!-- AI_ASSISTED_DOCUMENTATION_NOTICE -->\n'
              '> **AI-assisted documentation:** This document is developed with assistance from ChatGPT AI, '
             